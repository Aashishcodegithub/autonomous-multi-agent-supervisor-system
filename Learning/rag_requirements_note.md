# Day 15 RAG dependencies

This repo currently installs only the base LangChain/LangGraph deps (per README).
For Day 15 you also need:

- numpy

It uses `GoogleGenerativeAIEmbeddings` from `langchain-google-genai`.

Install (in your venv):

```bash
pip install numpy
```

